# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- Agente / orquestacion (open source) ---
langchain==1.4.0
langgraph==1.2.11
langchain-core==1.6.2
langchain-ollama==1.1.0
ollama==0.6.2

# --- Proveedores de LLM alternativos (opcionales, LLM_PROVIDER=openai|anthropic) ---
langchain-openai==1.6.0
langchain-anthropic==1.7.1

# --- Persistencia (open source) ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
pgvector==0.3.5
redis==8.1.0
# Memoria de corto plazo: checkpointer de LangGraph sobre Redis (thread-level, por session_id).
# Requiere el modulo RediSearch -> imagen redis:8 o redis-stack (ver docker-compose.yml).
# https://redis.io/blog/langgraph-redis-build-smarter-ai-agents-with-memory-persistence/
langgraph-checkpoint-redis==0.5.2

# --- Observabilidad (open source) ---
langfuse==2.51.3
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0

# --- Utilidades ---
httpx==0.28.1
tenacity==9.1.4

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0

# --- Evaluacion del agente (LLM-as-judge, open source) ---
deepeval==4.2.2
